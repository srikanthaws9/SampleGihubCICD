name: Deploy code to dev env
# new deploy code
on:
  push:  
    branch: 
      - main
      - dev
      - feature/cicd-setup
      - uat
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy the code
        run: echo " Deployed the code welcome to DevOps " asdf asdf asdf sasdf 
  
    
